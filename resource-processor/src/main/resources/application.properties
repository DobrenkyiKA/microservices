# All configurations are fetched from Config Server
# This file can be left empty or contain only bootstrap-level configs
spring.application.name=resource-processor

server.port=8086

# Kafka configuration
resource.processing.completion.topic=resource-processing-completed